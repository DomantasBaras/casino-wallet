memory_limit = 512M
max_execution_time = 60

; Keep opcache off locally so code changes are picked up without a restart.
opcache.enable = 0

; Money is decimal. Serialize precision high enough that nothing silently rounds
; on the way through JSON.
serialize_precision = -1

display_errors = On
error_reporting = E_ALL
